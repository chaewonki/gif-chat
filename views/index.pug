extends layout

block content
  div F12를 눌러 console 탭과 network 탭을 확인하세요.
  //- script provided by Socket.IO to client
  //- connect with server address through io object provided the script
  //- when connect, address starts with http not ws
  //- with Socket.IO, connect server with polling, after polling connection, if web socket is possible, then connect with web socket
  script(src='/socket.io/socket.io.js') 
  script.
    var socket = io.connect('http://localhost:8005',{
        path:'/socket.io',
    });
    socket.on('news',function(data){
        console.log(data);
        socket.emit('reply','Hello Node.JS');
    });
    //- var webSocket = new WebSocket("ws://localhost:8005");
    //- webSocket.onopen = function () {
    //-   console.log('서버와 웹소켓 연결 성공!');
    //- }
    //- webSocket.onmessage = function (event) {
    //-   console.log(event.data);
    //-   webSocket.send('클라이언트에서 서버로 답장을 보냅니다');
    //- }